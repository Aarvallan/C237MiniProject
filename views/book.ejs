<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <title><%= books.name %> - Bookstore App</title>
    <style>
        body {
            background-image: url('/images/<%= books.gif %>');
            background-size: cover;
            background-repeat: no-repeat;
            background-attachment: fixed;
            color: darkblue; 
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; 
            font-size: 1rem; 
        }
        h1, h2, h3, h4, h5, h6 {
            color: darkblue; 
        }
        .btn-primary, .btn-danger, .btn-outline-primary, .btn-outline-danger {
            font-size: 1rem; 
        }
        .btn-primary {
            background-color: darkblue; 
            border-color: darkblue; 
        }
        .btn-primary:hover {
            background-color: #0d6efd; 
            border-color: #0d6efd; 
        }
        .btn-danger {
            background-color: #dc3545; 
            border-color: #dc3545; 
        }
        .btn-danger:hover {
            background-color: #c82333; 
            border-color: #c82333; 
        }
    </style>
</head>
<body>
    
    <div class="container mt-4">
        <div class="row">
            <div class="col-md-6">
                <h1><%= books.name %></h1>
                <p><strong>Quantity:</strong> <%= books.quantity %></p>
                <p><strong>Price:</strong> $<%= books.price %></p>
                <p><strong>Description:</strong> <%= books.description %></p>
                <p><strong>Genre:</strong> <%= books.genre %></p>
                <form action="/add-to-cart/<%= books.bookId %>" method="POST" class="mb-3">
                    <div class="input-group mb-3">
                        <label for="quantity" class="input-group-text">Quantity</label>
                        <input type="number" class="form-control" id="quantity" name="quantity" value="1" min="1" required>
                    </div>
                    <button type="submit" class="btn btn-primary btn-lg">Add to Cart</button>
                </form>
                <a class="btn btn-outline-primary me-2" href="/editbooks/<%= books.bookId %>">Edit</a>
                <a class="btn btn-outline-danger me-2" href="/deletebook/<%= books.bookId %>" onclick="return confirm('Do you wish to delete this book?')">Delete</a>
                <a class="btn btn-outline-primary" href="/">Back</a>
            </div>
        </div>
    </div>
</body>
</html>
